#include <stdio.h>
#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: the character
 * Return: 1 on success 0 otherwise
 */

int _isupper(int c);
{
	char c;

	if (c == "C")
	{
		return (1);
	}
	else if (c == "c")
	{
		return (0);
	}
}
